# QtPdf tiled rendering and freezing are verified with PyQt6 6.8–6.9.
# Qt 6.10.1+ Windows wheels require an ICU runtime that is not bundled by
# PyInstaller, so keep release builds below 6.10 until that is resolved.
PyQt6>=6.8,<6.10
imageio-ffmpeg>=0.6,<1
Pillow>=11,<13
pypdf>=6,<7
PyInstaller>=6.3,<7
certifi>=2026.2.25
